server.port=8080

# H2 Configuration (In-Memory for testing)
spring.datasource.url=jdbc:h2:mem:custodydb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true

# PostgreSQL Configuration (Disabled)
# spring.datasource.url=jdbc:postgresql://localhost:5432/custodydb
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# H2 Console (Disabled for production)
spring.h2.console.enabled=false
